# Environment / secrets
.env
.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
.pytest_cache/

# State files (may contain job IDs but not sensitive; still exclude to avoid noise)
seen_jobs.json
/tmp/

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfvars

# Docker
*.tar

# macOS
.DS_Store

# IDE
.idea/
.vscode/
